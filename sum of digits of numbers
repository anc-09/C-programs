include <stdio.h>

int main() 
{
    int num,num=0,dg;
    printf("Enter a number:");
    scanf("%d",&num);
    while(num>0)
    {
        dg=num%10;
        sum+=dg;
        num=num/10;
    }
    printf("\nSum of digits is %d",sum);
    return 0;
}
